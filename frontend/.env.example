# Base URL of the VoicePrint FastAPI backend.
# All requests are sent to ${NEXT_PUBLIC_API_URL}/api/v1/...
# Run the backend locally per DEPLOY.md:
#   cd backend && uvicorn voiceprint.main:app --host 0.0.0.0 --port 8000
NEXT_PUBLIC_API_URL=http://localhost:8000
