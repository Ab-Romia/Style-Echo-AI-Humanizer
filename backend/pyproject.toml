[tool.poetry]
name = "voiceprint"
version = "0.1.0"
description = "AI Text Humanizer with Style Learning"
authors = ["Ab-Romia <aabouroumia@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.10"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
